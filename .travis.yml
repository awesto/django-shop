dist: xenial

language: python

python:
  - 3.6

install:
  - pip install 'Django<3.0'
  - pip install --no-deps -r tests/requirements.txt
  - pip install -e .
  - pip freeze

script:
  - export DJANGO_DEBUG=1 cd tests && py.test
